<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epsilon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".EpsilonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".screens.main.MainActivity">

        </activity>

        <activity
            android:name=".screens.login.LoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".screens.register.RegisterActivity"
            android:theme="@style/AppTheme.Dark">

        </activity>

        <activity android:name=".screens.categorydetail.CategoryDetailActivity">


        </activity>

        <activity android:name=".screens.coursedetail.CourseDetailActivity">

        </activity>

    </application>

</manifest>
